const currentSpec = require('../current-spec/index.json');
const {
  StructuralValueMarkingSkill,
  calibrateStructuralValue
} = require('./structural-value-marking');

module.exports = {
  currentSpec,
  StructuralValueMarkingSkill,
  calibrateStructuralValue
};
